services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_remote360
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network

volumes:
  mongo-data:

networks:
  app-network:
